{% extends 'base.html.twig' %}

{% block title %}"Ma page"{% endblock %}

{% block content %}

    <h1>La boutique du gourmand</h1>

    <div class="container">
        <div class="row justify-content-between">

            {% for product in products %}
                <div class="col-4">
                    <div class="card" style="width: 18rem; margin-top: 2rem">
                        <img src="../../public/img/confiserie.jpg" class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title">Nouveauté</h5>
                            <p class="card-text">{{ product }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>

{% endblock %}